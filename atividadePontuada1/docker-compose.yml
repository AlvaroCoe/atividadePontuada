version: '3.8'
services:
  db:
    image: mysql:8.0
    container_name: atividadepontuada
    restart: always
    environment:
      MYSQL_DATABASE: atividade_pontuada
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"